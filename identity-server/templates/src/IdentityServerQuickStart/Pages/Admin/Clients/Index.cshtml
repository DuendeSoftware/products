@page
@model IdentityServerHost.Pages.Admin.Clients.IndexModel

<div class="container-fluid mb-4 px-0">
    <div class="d-flex justify-content-between align-items-center bg-body-tertiary rounded shadow-sm p-2 flex-nowrap">

        <!-- Page title -->
        <nav aria-label="breadcrumb" class="mb-0 ms-2">
            <ol class="breadcrumb mb-0 p-0 fs-5">
                <li class="breadcrumb-item active" aria-current="page">Clients</li>
            </ol>
        </nav>

        <!-- Actions: keep dropdown + form together, no wrap -->
        <div class="d-flex align-items-center flex-nowrap gap-2">

            <!-- Create dropdown -->
            <div class="dropdown">
                <button class="btn btn-link dropdown-toggle text-decoration-none"
                        type="button"
                        data-bs-toggle="dropdown"
                        aria-expanded="false">
                    Create Client
                </button>
                <ul class="dropdown-menu dropdown-menu-end">
                    <li>
                        <a class="dropdown-item" href="/Admin/Clients/New?type=web">
                            Web
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item" href="/Admin/Clients/New?type=m2m">
                            Machine-to-Machine
                        </a>
                    </li>
                </ul>
            </div>

            <!-- Filter/Search form -->
            <form class="d-flex align-items-center flex-nowrap mb-0" role="search">
                <input class="form-control me-2"
                       type="text"
                       id="filter"
                       name="filter"
                       placeholder="Filter"
                       value="@Model.Filter" />

                <button class="btn btn-outline-success" type="submit">
                    Search
                </button>
            </form>

        </div>
    </div>
</div>


<table class="table mt-4">
    <thead class="table-light">
        <tr>
            <th>Client ID</th>
            <th>Name</th>
            <th>Flow</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var client in Model.Clients)
        {
            <tr>
                <td>
                    <a href="javascript:void(0)"
                       class="client-edit-link"
                       data-client-id="@client.ClientId">
                        @client.Name
                    </a>
                </td>
                <td>@client.Name</td>
                <td>@client.Flow</td>
            </tr>
        }
    </tbody>

</table>

<!-- Bootstrap Modal -->
<div class="modal fade" id="editClientModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Edit Client</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body" id="editClientModalBody">
                <!-- Partial loads here -->
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/clients/index.js"></script>
}
