// <auto-generated by="ValueObjectsGenerator"/>
// Copyright (c) Duende Software. All rights reserved.
// See LICENSE in the project root for license information.
#nullable enable

using System.Collections.Generic;
using Duende.Storage;
using System.Globalization;
using System.Diagnostics.CodeAnalysis;

namespace Duende.Storage.Pagination;

[System.ComponentModel.TypeConverter(typeof(ValueOfTypeConverter<DataRangeSize, global::System.Int32>))]
partial record DataRangeSize : IValueOf<DataRangeSize, global::System.Int32>
{
    // Constructor for controlled creation
    private DataRangeSize(global::System.Int32 value) => Value = value;

    public global::System.Int32 Value { get; }

    public static DataRangeSize Create(string s)
    {
        if (!TryCreate(s, out var result, out var errors))
        {
            throw new FormatException($"The value '{s}' is not a valid '{nameof(DataRangeSize)}'. {string.Join(" ", errors)}");
        }
        return result;
    }

    public static bool TryCreate(string? s, [NotNullWhen(true)] out DataRangeSize? result)
        => TryCreate(s, out result, out _);

    public static bool TryCreate(string? s, [NotNullWhen(true)] out DataRangeSize? result, [NotNullWhen(false)] out IReadOnlyList<string>? errors)
    {
        result = null;
        errors = null;
        if (string.IsNullOrWhiteSpace(s))
        {
            errors = ["A value is required."];
            return false;
        }

        if (global::System.Int32.TryParse(s, CultureInfo.InvariantCulture, out var value))
        {
            if (!TryValidate(value, out var tryValidateErrors))
            {
                errors = tryValidateErrors is { Count: > 0 } ? tryValidateErrors : [$"The value is not a valid '{nameof(DataRangeSize)}'."];
                return false;
            }
            var instance = new DataRangeSize(value);
            result = instance;
            return true;
        }

        errors = ["The value could not be parsed."];
        return false;
    }

    public static implicit operator DataRangeSize(global::System.Int32 value)
    {
        if (!TryValidate(value, out var errors))
        {
            var errorMessage = $"The value '{value}' is not a valid '{nameof(DataRangeSize)}'. {string.Join(" ", errors ?? [])}";
            throw new FormatException(errorMessage);
        }
        return new DataRangeSize(value);
    }

    public override string ToString() => Value.ToString(null, CultureInfo.InvariantCulture);

    public static DataRangeSize? CreateOrDefault(string? input)
    {
        if (string.IsNullOrEmpty(input))
        {
            return null;
        }

        return Create(input);
    }

    internal static DataRangeSize Load(global::System.Int32 value) => new DataRangeSize(value);
}
