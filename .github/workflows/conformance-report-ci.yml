# This was generated by tool. Edits will be overwritten.

name: conformance-report/ci
on:
  workflow_dispatch:
  push:
    paths:
    - .config/dotnet-tools.json
    - .github/workflows/conformance-report-**
    - conformance-report/**
    - identity-server/src/IdentityServer.ConformanceReport/**
    - .editorconfig
    - Directory.Packages.props
    - global.json
    - src.props
    - test.props
  pull_request:
    paths:
    - .config/dotnet-tools.json
    - .github/workflows/conformance-report-**
    - conformance-report/**
    - identity-server/src/IdentityServer.ConformanceReport/**
    - .editorconfig
    - Directory.Packages.props
    - global.json
    - src.props
    - test.props
env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
jobs:
  verify-formatting:
    name: Verify formatting
    if: (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository) || (github.event_name == 'push') || (github.event_name == 'workflow_dispatch')
    runs-on:
      group: large
      labels: [ubuntu-latest-x64-16core]
    permissions:
      contents: read
    defaults:
      run:
        shell: bash
        working-directory: conformance-report
    timeout-minutes: 15
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: List .net sdks
      run: dotnet --list-sdks
    - name: Setup Dotnet
      uses: actions/setup-dotnet@3e891b0cb619bf60e2c25674b222b8940e2c1c25
      with:
        dotnet-version: 10.0.100
    - name: Restore
      run: dotnet restore conformance-report.slnf
    - name: Verify Formatting
      run: dotnet format conformance-report.slnf --verify-no-changes --no-restore
  build:
    name: Build and test (unit)
    if: (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository) || (github.event_name == 'push') || (github.event_name == 'workflow_dispatch')
    runs-on:
      group: large
      labels: [ubuntu-latest-x64-16core]
    permissions:
      actions: read
      checks: write
      contents: read
      packages: write
    defaults:
      run:
        shell: bash
        working-directory: conformance-report
    timeout-minutes: 15
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: List .net sdks
      run: dotnet --list-sdks
    - name: Setup Dotnet
      uses: actions/setup-dotnet@3e891b0cb619bf60e2c25674b222b8940e2c1c25
      with:
        dotnet-version: 10.0.100
    - name: Restore
      run: dotnet restore conformance-report.slnf
    - name: Build
      run: dotnet build conformance-report.slnf --no-restore -c Release
    - name: Dotnet devcerts
      run: dotnet dev-certs https --trust
    - name: Test - test/ConformanceReport.Tests
      run: >-
        dotnet run --project test/ConformanceReport.Tests -c Release --no-build -- \
          --report-xunit-trx --report-xunit-trx-filename ConformanceReport.Tests.trx \
          --coverage --coverage-output-format cobertura \
          --coverage-output ConformanceReport.Tests.cobertura.xml
    - id: test-report-test-ConformanceReport-Tests
      name: Test report - test/ConformanceReport.Tests
      if: github.event_name == 'push' && (success() || failure())
      uses: dorny/test-reporter@31a54ee7ebcacc03a09ea97a7e5465a47b84aea5
      with:
        name: Test Report - test/ConformanceReport.Tests
        path: '**/ConformanceReport.Tests.trx'
        reporter: dotnet-trx
        fail-on-error: true
        fail-on-empty: true
    - name: Publish test report link
      run: echo "[Test Results - test/ConformanceReport.Tests](${{ steps.test-report-test-ConformanceReport-Tests.outputs.url_html }})" >> $GITHUB_STEP_SUMMARY
  playwright:
    name: Playwright tests
    if: (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository) || (github.event_name == 'push') || (github.event_name == 'workflow_dispatch')
    runs-on:
      group: large
      labels: [ubuntu-latest-x64-16core]
    permissions:
      actions: read
      checks: write
      contents: read
    defaults:
      run:
        shell: bash
        working-directory: conformance-report
    timeout-minutes: 15
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
  codeql:
    name: CodeQL analyze
    if: (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository) || (github.event_name == 'push') || (github.event_name == 'workflow_dispatch')
    runs-on:
      group: large
      labels: [ubuntu-latest-x64-16core]
    defaults:
      run:
        shell: bash
        working-directory: conformance-report
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
  pack:
    name: Pack, sign and push
    if: (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository) || (github.event_name == 'push') || (github.event_name == 'workflow_dispatch')
    needs:
    - verify-formatting
    - build
    - playwright
    - codeql
    runs-on:
      group: large
      labels: [ubuntu-latest-x64-16core]
    permissions:
      actions: read
      contents: read
      packages: write
    defaults:
      run:
        shell: bash
        working-directory: conformance-report
    timeout-minutes: 15
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: List .net sdks
      run: dotnet --list-sdks
    - name: Setup Dotnet
      uses: actions/setup-dotnet@3e891b0cb619bf60e2c25674b222b8940e2c1c25
      with:
        dotnet-version: 10.0.100
    - name: Tool restore
      run: dotnet tool restore
    - name: Pack conformance-report.slnf
      run: dotnet pack -c Release conformance-report.slnf -o artifacts
    - name: Sign packages
      if: github.event == 'push'
      run: >-
        for file in artifacts/*.nupkg; do
           dotnet NuGetKeyVaultSignTool sign "$file" --file-digest sha256 --timestamp-rfc3161 http://timestamp.digicert.com --azure-key-vault-url https://duendecodesigninghsm.vault.azure.net/ --azure-key-vault-client-id 18e3de68-2556-4345-8076-a46fad79e474 --azure-key-vault-tenant-id ed3089f0-5401-4758-90eb-066124e2d907 --azure-key-vault-client-secret ${{ secrets.SignClientSecret }} --azure-key-vault-certificate NuGetPackageSigning
        done
    - name: Push packages to GitHub
      if: github.ref == 'refs/heads/main'
      run: dotnet nuget push artifacts/*.nupkg --source https://nuget.pkg.github.com/DuendeSoftware/index.json --api-key ${{ secrets.GITHUB_TOKEN }} --skip-duplicate
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        NUGET_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Upload Artifacts
      if: github.ref == 'refs/heads/main'
      uses: actions/upload-artifact@b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882
      with:
        name: artifacts
        path: conformance-report/artifacts/*.nupkg
        overwrite: true
        retention-days: 15
